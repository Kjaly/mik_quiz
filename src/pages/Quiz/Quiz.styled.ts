import styled from 'styled-components';


export const StyledQuiz = styled.div`
  width: 100%;
  background: linear-gradient(0deg, ${({theme}) => theme.color.darkwhite} 80%, rgba(0, 0, 0, 0) 50%);
`

export const StyledTitleDescription = styled.div`
  line-height: 1.5;
  font-size: 15px;
  font-weight: 400;
  max-width: 620px;

`
